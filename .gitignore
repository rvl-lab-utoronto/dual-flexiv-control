__pycache__/
*.pyc
.venv/
.env

# Build artifacts
*.egg-info/
build/
dist/

# Runtime: shared-memory stream manifests / pid files (per-run, ephemeral)
# Anchored to repo root so it does NOT also ignore conf/runtime/ (Hydra config group).
/runtime/

# Hydra run outputs
outputs/
multirun/

# Editor / OS
.idea/
.vscode/
.DS_Store

# Large test fixtures (do not commit)
rizon_tests.zip

# Dataset videos over GitHub's 50 MB limit (kept locally, not in git)
datasets/dfc/default/videos/observation.images.static_left/chunk-000/file-001.mp4
datasets/dfc/default/videos/observation.images.static_right/chunk-000/file-001.mp4
